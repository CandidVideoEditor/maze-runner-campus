# Maze Runner & Catcher — Campus Edition

Playful campus-themed maze chase built with Pygame.

## Quick start

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python -m game.maze_runner
